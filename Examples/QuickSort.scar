program QuickSort;

var
  a: array of Integer;
  n, i: Integer;

begin
  ClearDebug;
  a := [100, 90, 80, 70, 60, 50, 91, 42, 85, 76];
  QuickSort(a);
  n := Length(a);
  for i := 0 to n - 1 do
    WriteLn(IntToStr(a[i]));
end.