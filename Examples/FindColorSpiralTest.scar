program New;
var
  x, y: Integer;
begin
  x := 70;
  y := 70;              
  
  if(FindColorSpiral(0, 0, x, y, clBlack, 0, 0, 100, 100))then
    Writeln('Black color found at ' + IntToStr(x) + ',' + IntToStr(y))
  else
    Writeln('Black color not found in upper left corner');
end.
